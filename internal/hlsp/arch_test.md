manager -> docs -> queries/stuff

docs -> types and doc.symbols

How it will access global types?

Static global types and symbols?

---

To match scope I think we should go through the tree and create 
a symbol hierachy based on that symbols oerder
